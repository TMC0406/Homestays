const initState = {
   isLoggedIn: false,
   token: null
  }
  
  const authSlides = (state = initState, action) => {
    switch (action.type) {
      
    
      default:
        return state;
    }
  }
  
  export default authSlides