const initState = {
    post: []
}

const postSlides = (state = initState, action) => {
    switch (key) {
        // case value:
            
        //     break;
    
        default:
            return state;
    }
}

export default postSlides