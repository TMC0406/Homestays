import { rejects } from "assert";
import { resolve } from "path";



const apiRegister = (payload) => new Promise(async(resolve, rejects) => {
    try{
        const response = await 
    }catch(error){
        rejects(error)
    }
}) 